# Only needed for `npm run dev` if your FastAPI backend (backend/server.py)
# is NOT running on http://127.0.0.1:8000 -- e.g. developing from a
# laptop while the backend runs on the Android device on your LAN.
# VITE_BACKEND_URL=http://192.168.1.50:8000
